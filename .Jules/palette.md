# Palette's Journal

## 2024-05-22 - Financial Input Readability
**Learning:** Users struggle to verify large numbers (millions/billions) in standard number inputs without visual formatting.
**Action:** Always pair `input type="number"` for currency with a real-time formatted helper text element linked via `aria-describedby`.
