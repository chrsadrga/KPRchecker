## 2026-02-06 - Financial Input Readability
**Learning:** Native `input type="number"` is accessible but poor for large currency values (hard to count zeros).
**Action:** Always pair financial inputs with real-time formatted helper text linked via `aria-describedby`.
